<%= render 'shared/banner' %>

<div class="row grid p-5">
  <% @fields.each do |field| %>
    <%= link_to "#{field_choices_path}?field_id=#{field.id}", method: :post do %>
      <%= render "shared/card", field: field %>
    <% end %>
  <% end %>
</div>

